# Functors — established

The learner completed Lesson 2 and the from-memory definition exercise: a functor `𝒞 → 𝒟` as object assignment + morphism assignment satisfying functoriality (preserves composition `F(fg)=F(f) F(g)`, preserves identities). Functor / functoriality / covariant functor / Cat are now promoted into the glossary.

**Implications:** Functors are a solid floor. Next is the opposite category `𝒞^op` and contravariant functors, then natural transformations — the remaining steps before the Yoneda machinery the mission targets. The hom-functor `𝒞(A,−)` was introduced as the gateway to Yoneda and should be reinforced (it's in the review queue). Durability still to be confirmed by the spaced queue items (due 2026-06-17).
